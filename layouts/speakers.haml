%script{:id => 'speakers-template', :type => 'text/x-handlebars-template'}
  {{#each speakers}}
  .speaker{:style => 'background-image: url({{photo_url}})'}
    .description
      .info
        %h2 {{name}}
      .content
        {{bio}}
  {{/each}}

